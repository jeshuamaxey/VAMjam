angular.module('mean.play').controller('PlayController', ['$scope', '$routeParams', '$location', 'Global', 'Play', function ($scope, $routeParams, $location, Global, Play) {
    $scope.global = Global;

   
}]);